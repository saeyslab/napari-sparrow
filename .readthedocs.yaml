# https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11.15"

conda:
  environment: environment.yml

submodules:
  include:
    - docs/tutorials
  recursive: false    # only needed if harpy_notebooks itself has submodules

sphinx:
  configuration: docs/conf.py
  # disable this for more lenient docs builds
  # fail_on_warning: true
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev
        - docs
