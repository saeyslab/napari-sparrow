# @package _global_

# see here for the docs:
#https://hydra.cc/docs/patterns/configuring_experiments/

defaults:
  - override /dataset: vizgen_example
  - override /segmentation: cellpose_vizgen_example
  - override /paths: output
  - override /clean: vizgen_example
  - override /allocate: vizgen_example
#subset: '0:2144,0:2144'
